# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
env:
  MONGO_URI: mongodb+srv://ellasyoung:Nps1300845!@crochetcompassv1.4wmpnex.mongodb.net/crochetpatterns?retryWrites=true&w=majority
  
name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CROCHETCOMPASS }}
          channelId: live
          projectId: crochetcompass
  build_and_deploy2:          
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci --prefix './client' && npm run build --prefix './client'
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CROCHETCOMPASS }}
          channelId: live
          projectId: crochetcompass
